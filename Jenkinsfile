Jenkinsfile (Scripted pipeline)

node {

    stage('Example'){
        if(env.BRANCH_NAME=='master'){
            echo 'execute master'
        }else{
            echo 'execute elsewhere'
        }
    }
}

